# Connect to sinus
ssh root@sinus.bluetree.ie
# Get a shell in the container
docker exec -it docker-qa-fdroid bash
# cd to the fdroid directory for the channel
cd /data/fdroidserver/<channel name>/fdroid
fdroid update

QA Appus 
ssh ubuntu@10.129.153.161 -i qa.pem 
cd /srv/docker-fdroid 
docker-compose up --build -d


/srv/docker-fdroid/data/fdroidserver-secret

Esquared SOTI Enrollment ID
MCNCFY63
